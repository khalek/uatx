apply plugin: 'eclipse'
apply plugin: 'groovy'

dependencies {
    compile (
        localGroovy(),
        'com.android.tools.ddms:ddmlib:22.0'
    ) 
}

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper)